import { SignupForm } from '@/components/auth/signup-form';

export const metadata = {
    title: 'Sign Up | Sales Intelligence Dashboard',
    description: 'Create a new account',
};

export default function SignupPage() {
    return <SignupForm />;
}
